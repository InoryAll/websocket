/**
 * reducer
 * Created by tianrenjie on 2018/3/6
 */

const initFields = {};
export function message(state = initFields, action) {
  switch (action.type) {
    case '':
    default:
      return state;
  }
}
